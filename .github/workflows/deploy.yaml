name: Deploy to ArgoCD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set ArgoCD Server
        run: echo "ARGOCD_SERVER=192.168.1.140:32384" >> $GITHUB_ENV

      - name: ArgoCD Login
        run: |
          argocd login $ARGOCD_SERVER --username admin --password 1q2w3e4r --insecure

      - name: Sync ArgoCD Application
        run: |
          argocd app sync my-app
